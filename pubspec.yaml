name: mediroster
description: A cross-platform medical roster management application for Android and iOS
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase
  firebase_core: ^4.5.0
  firebase_auth: ^6.2.0
  cloud_firestore: ^6.1.3
  
  # State Management
  provider: ^6.1.2
  
  # Utilities
  intl: ^0.20.2
  
  # UI/UX
  flutter_spinkit: ^5.2.1
  fluttertoast: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  
  # Assets
  # assets:
  #   - images/
  
  # Fonts
  # fonts:
  #   - family: BrunoAceSC
  #     fonts:
  #       - asset: fonts/BrunoAceSC-Regular.ttf
